export class Food {
    static foodList = ["basic", "minusScore", "disappear", "cutLength"];
    constructor(x, y){
        this.x = x;
        this.y = y;
        this.width = 50
        this.height = 50
    }
}